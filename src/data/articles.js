import { faCode, faGaugeHigh, faGlobe, faLayerGroup, faServer } from "@fortawesome/free-solid-svg-icons";

const siteUrl = "";
const section = (heading, paragraphs, table) => ({ heading, paragraphs, table });
const makeArticle = (article) => {
  const words = article.body.map(({ heading, paragraphs }) => `${heading} ${paragraphs.join(" ")}`).join(" ").trim().split(/\s+/).length;
  return { ...article, status: "published", publicationDate: "2026-09-02", updatedDate: "2026-09-02", wordCount: words, readingTime: `${Math.max(1, Math.ceil(words / 220))} min read`, canonicalUrl: `${siteUrl}/blog/${article.slug}`, openGraphImage: `${siteUrl}/assets/ali-logo.svg` };
};

const articles = [
  makeArticle({
    slug: "grpc-vs-rest-modern-services", title: "gRPC vs REST: Choosing the Right Communication Layer for Modern Services", category: "Backend Architecture", tags: ["gRPC", "REST APIs", "Microservices", "Protocol Buffers", "Backend Architecture"], excerpt: "REST remains an excellent default for public APIs, but internal service communication can have different requirements. This article compares REST and gRPC through performance, contracts, streaming, debugging, browser support, and operational complexity.", seoTitle: "gRPC vs REST for Modern Services | Ali Almanea", seoDescription: "A practical comparison of REST and gRPC covering contracts, streaming, browser support, observability, versioning, and service boundaries.", icon: faServer, body: [
      section("Start with the boundary, not the protocol", ["REST is still the right default for many public APIs. It is familiar to browsers, mobile clients, partners, and debugging tools. HTTP semantics, resource-oriented URLs, caching, and broad documentation make it inexpensive to operate across organizational boundaries.", "The question changes inside a system. An internal call may need a strict contract, low serialization overhead, streaming, or generated clients across several services. Those are real reasons to consider gRPC, but they are not reasons to replace every HTTP endpoint. The communication layer should follow the boundary and failure model."]),
      section("Contracts and payloads", ["gRPC commonly uses Protocol Buffers: a compact schema that can generate client and server code. The schema becomes an explicit contract for fields, types, and service methods. That can make refactoring safer when several teams consume the same internal service.", "REST can be strongly contracted too, with OpenAPI, validation, and generated clients. The difference is less about whether a contract exists and more about how tightly transport and generated code shape the workflow. Protobuf serialization is compact and efficient, while JSON is easier to inspect and integrate manually. Payload size matters only after measuring the workload and network path."]),
      section("Streaming is a first-class choice", ["gRPC supports unary calls, server streaming, client streaming, and bidirectional streaming. That model is useful for long-running feeds, telemetry, incremental processing, and interactive service-to-service workflows. Streaming changes lifecycle, backpressure, retries, and monitoring requirements.", "REST can support streaming through chunked responses, server-sent events, or WebSockets. These can be better when a browser is the primary client or an HTTP-native interface is more accessible. Streaming is an architectural decision, not merely a performance switch."]),
      section("A balanced comparison", ["The practical differences are tradeoffs rather than a universal ranking."], [["Concern", "REST", "gRPC"], ["Public integration", "Broadest compatibility", "Usually needs a gateway"], ["Contract", "OpenAPI and conventions", "Protobuf and generated contracts"], ["Debugging", "Easy with browser and curl", "Needs suitable tooling"], ["Streaming", "Several HTTP patterns", "Built into RPC model"], ["Internal calls", "Simple and capable", "Efficient typed boundaries"]]),
      section("Browsers, observability, and evolution", ["Direct browser support is a decisive constraint. Native gRPC is not the same thing as a JSON fetch. gRPC-Web or a gateway can bridge that gap, but the bridge adds another component and contract to observe.", "Whichever protocol is chosen, trace IDs, request duration, error categories, payload limits, and dependency health need to be visible. Generated clients do not remove the need for timeouts or clear retry policy. Protobuf fields can evolve safely when old fields remain readable and new fields are compatible. REST needs the same discipline through additive changes and deprecation."]),
      section("A decision checklist", ["Choose REST when external accessibility, browser clients, partner integration, human-readable debugging, or straightforward caching are priorities. Consider gRPC when a team controls both sides of an internal boundary, strong generated contracts reduce meaningful risk, payload efficiency has been measured as important, or streaming is central.", "Avoid introducing gRPC only because it sounds faster or more modern. It can add gateway configuration, certificate and HTTP/2 concerns, unfamiliar debugging paths, and generated-code coordination. Use REST when accessibility and simplicity matter most; consider gRPC when typed internal communication or streaming is a genuine requirement."]),
    ]
  }),
  makeArticle({
    slug: "t3-stack-speed-and-tradeoffs", title: "The T3 Stack: Where It Speeds Up Development—and Where It Adds Coupling", category: "Full Stack Engineering", tags: ["Next.js", "TypeScript", "tRPC", "Tailwind CSS", "Full Stack"], excerpt: "The T3 Stack can provide an excellent TypeScript developer experience, but speed is not the only architectural concern. Here is where end-to-end types help, where coupling appears, and when a different stack may be healthier.", seoTitle: "The T3 Stack: Speed, Type Safety, and Coupling | Ali Almanea", seoDescription: "An honest guide to the T3 ecosystem, including Next.js, TypeScript, tRPC, Prisma, Tailwind, validation, testing, and service boundaries.", icon: faCode, body: [
      section("What people mean by T3", ["T3 is best understood as a style of building a TypeScript product around Next.js, TypeScript, tRPC, Prisma, Tailwind CSS, authentication, and runtime validation. The exact composition varies. The value is a coherent developer experience, not a mandatory bundle.", "Next.js and TypeScript are verified parts of my professional toolkit, as are tRPC and Tailwind CSS. Prisma is common in the ecosystem, but it should be evaluated on project needs rather than assumed as professional experience. That distinction matters when describing architecture honestly."]),
      section("Where the speed comes from", ["End-to-end types reduce the gap between a server procedure and the UI that consumes it. A renamed field can produce a compile-time signal in more places, and a feature often needs less request and response plumbing. This is useful when one team owns the product and can change both sides together.", "TypeScript types disappear at runtime, so schemas still need to reject malformed requests and protect boundaries. Fast delivery comes from aligned tools, good defaults, and small feedback loops—not a framework label."]),
      section("The coupling tax", ["The same connection that makes a feature quick can make a boundary harder to change. A public client, mobile app, or partner may not consume a private procedure directly. A tRPC-shaped API can make frontend code depend on server naming and deployment choices more closely than a stable REST contract.", "Internal screens may benefit from this coupling. Public APIs, long-lived integrations, background jobs, and independently deployed services often benefit from explicit contracts and asynchronous messages instead. Prisma can simplify data access, but migrations, query shape, and transactions still require database judgment."]),
      section("A practical decision matrix", ["Use this as a starting point rather than a prescription."], [["Situation", "T3-style approach", "Alternative"], ["One team owns web UI and API", "Strong fit", "REST if public"], ["Public or partner API", "Add stable boundary", "REST/OpenAPI"], ["Independent services", "Keep contracts explicit", "REST, gRPC, or events"], ["Heavy background work", "Pair with queues", "Worker service"], ["Small product team", "Fast defaults", "Fewer libraries"]]),
      section("Testing and asynchronous work", ["Types do not replace tests. Procedure tests should cover authorization and validation; integration tests should exercise database behavior; browser tests should verify important user journeys. Contract tests help when client and server deploy separately.", "Email, imports, AI processing, and long-running reports need queues, retries, idempotency, and visible failure states. The T3 stack can sit beside those systems, but it does not solve them automatically."]),
      section("When to choose or avoid it", ["Choose a T3-style stack when a TypeScript team owns the product end to end, iteration speed matters, and coupling is deliberate. Keep validation at runtime, establish a data-access boundary, and decide which routes are public.", "Avoid it as a reflex for a multi-team platform or integration-heavy API whose clients must evolve independently. Start with the smallest coherent set of tools and make the important boundary clear."]),
    ]
  }),
  makeArticle({
    slug: "unified-core-api-architecture", title: "From Duplicated Logic to a Unified Core API", category: "System Design", tags: ["API Architecture", "Backend Systems", "Refactoring", "System Design", "SaaS"], excerpt: "When business rules are repeated across endpoints and services, every new feature becomes more fragile. This case study explains how a unified Core API can create one source of truth without becoming a new monolith.", seoTitle: "Designing a Unified Core API Without a Monolith | Ali Almanea", seoDescription: "An anonymized case study on consolidating duplicated business rules into a unified Core API with safer contracts, migration, and observability.", icon: faLayerGroup, body: [
      section("The problem was not the number of endpoints", ["Duplicated logic begins innocently. Two features need similar validation, so each endpoint implements it locally. A third consumer copies the rule, and the system develops several interpretations of one concept. Responses drift, edge cases diverge, and each change must be repeated carefully.", "This case study is based on verified work designing a unified Core API architecture, reducing duplicated business logic, and owning end-to-end delivery. Company-specific names, internal topology, and confidential details are intentionally omitted."]),
      section("Find the source of truth", ["List the rules that define the domain: validation, state transitions, authorization, calculations, and consistency requirements. Separate those from transport concerns such as status codes, serialization, pagination, and middleware. The goal is not one giant service; it is one deliberate place for each rule.", "A useful test is whether a rule could be called from an HTTP controller, worker, and scheduled job without changing its meaning. If it can, it likely belongs in a domain or application layer."]),
      section("Canonical contracts and thin projections", ["A unified API should publish canonical concepts and operations. Transport adapters can project those concepts into the shape needed by a screen, integration, or internal consumer. This avoids repeated computation while allowing purpose-built responses.", "The core must not become a dumping ground. Keep modules aligned to capabilities, define ownership, and make dependencies visible. A stable core with uncontrolled cross-module access is only a better-labeled monolith."]),
      section("Migration without breaking consumers", ["Migrate in slices. Introduce the new application service behind an existing endpoint, compare behavior in tests, and move one rule at a time. When response differences are intentional, publish a new contract rather than silently changing old consumers.", "Compatibility adapters are useful when they have an expiry plan. Track old callers, add deprecation visibility, and remove the adapter after dependencies move. Feature flags can control rollout, but should not hide two permanently different rules."]),
      section("Testing and operating the change", ["Unit tests should cover domain decisions without a database or HTTP server. Contract tests protect request and response shape. Integration tests verify persistence and transaction boundaries. Compare meaningful invariants rather than identical JSON when projections differ.", "Observability should identify route, core operation, consumer, version, and failure category. Traces locate slow dependencies, structured logs explain rejected transitions, and counters reveal active adapters. The CV supports improved scalability and stability, but no unsupported numerical improvement is claimed here."]),
      section("Lessons and tradeoffs", ["Centralizing rules reduces repetition, but can increase the blast radius of a poor change. Ownership, module boundaries, contract tests, and incremental migration are safeguards. A core API should make correct behavior easier to reuse, not make every team wait on a central team.", "The architecture earns its name when new features compose existing rules and consumers receive consistent behavior without hiding the cost of change."]),
    ]
  }),
  makeArticle({
    slug: "caching-queues-api-latency", title: "How Caching and Background Queues Reduce API Latency", category: "Backend Performance", tags: ["Redis", "Caching", "Background Jobs", "API Performance", "Node.js"], excerpt: "Slow APIs are not always solved by faster database queries. This case study explores how caching, queues, workers, and careful invalidation can move expensive work away from the request path.", seoTitle: "Caching, Queues, and API Latency | Ali Almanea", seoDescription: "A practical guide to measuring API bottlenecks, cache-aside design, queues, retries, idempotency, invalidation, and observability.", icon: faGaugeHigh, body: [
      section("Measure the request path first", ["Optimization begins with evidence. Measure endpoint latency by percentile, database time, external dependency time, payload size, and error rate. Averages hide slow requests. Trace a representative request before deciding that the database, cache, or queue is the answer.", "In documented CV work, backend optimization and caching strategies were associated with approximately 30% API performance improvement and approximately 35–40% latency reduction in the relevant context. Those are approximate, context-dependent outcomes—not guarantees for every system."]),
      section("Use cache-aside deliberately", ["With cache-aside, the application reads the cache first, loads the database on a miss, and writes the result back with a TTL. Redis is common because it is fast and supports useful data structures, but the cache should remain an optimization rather than the only source of truth.", "Keys need a stable naming scheme and enough scope to prevent collisions. TTLs should reflect how quickly data becomes stale. Never cache private data without including authorization scope in the key."]),
      section("Invalidation is the hard part", ["A cache hit is useful only when the value is acceptable. Write-through, explicit invalidation, versioned keys, and short TTLs solve different problems. The correct choice depends on whether stale data is inconvenient or unsafe.", "Avoid a cache that masks an unbounded query or unclear ownership. Stampedes, memory pressure, serialization cost, and stale reads can make a system worse. Instrument hit rate, miss latency, eviction, memory, and fallback frequency."]),
      section("Move non-critical work to queues", ["A request should not wait for work that does not determine its immediate response. Notifications, exports, indexing, media processing, and long-running AI tasks can be handled by workers. The API can acknowledge accepted work and expose status through a job resource.", "Queue architecture adds retries with backoff, idempotency keys, visibility timeouts, concurrency limits, and dead-letter handling. A worker must be safe to run twice because delivery is commonly at least once."]),
      section("Make failures visible", ["A queue is not a trash can for errors. Record attempts, reason codes, duration, and the destination of failed jobs. Alert on growing age and dead-letter volume, not just worker crashes. For caches, monitor hit rate and origin load together.", "Cron jobs and workers need the same ownership and observability as web requests. Clear timeouts and bounded work prevent one dependency from consuming the worker pool."]),
      section("A practical flow", ["A useful request path is client → API → cache lookup → database on miss → response. For expensive side effects, use client → API → enqueue job → immediate response → worker → durable result. Keep the synchronous contract small and choose consistency intentionally.", "Caching and queues reduce latency when they remove repeated or non-critical work from the critical path. They do not replace data modeling, indexes, capacity planning, or clear requirements."]),
    ]
  }),
  makeArticle({
    slug: "technical-seo-react-nextjs", title: "Technical SEO for React and Next.js: A Practical Engineering Checklist", category: "Technical SEO", tags: ["SEO", "React", "Next.js", "Web Performance", "Structured Data"], excerpt: "SEO is not a collection of meta tags. This practical guide covers rendering, crawlability, canonical URLs, structured data, internal linking, Core Web Vitals, and the technical checks that determine whether content can be discovered.", seoTitle: "Technical SEO Checklist for React and Next.js | Ali Almanea", seoDescription: "An engineering checklist for React and Next.js SEO covering rendering, metadata, canonical URLs, structured data, crawlability, and performance.", icon: faGlobe, body: [
      section("Start with page ownership", ["Before metadata, decide what query and user need a page owns. A technically perfect page with weak, duplicated, or unclear content will not become useful through markup. URL structure, internal links, and page purpose should be agreed before implementation.", "React applications can be SEO-friendly, but rendering strategy matters. Client rendering may leave crawlers and users waiting for JavaScript. Server rendering or static generation can expose meaningful HTML sooner when appropriate. Choose per page, not by slogan."]),
      section("Metadata and crawlability", ["Every important page needs a unique title and description. Set canonical URLs from the final public URL, normalize trailing slashes, and avoid unreliable request strings. Localized pages need deliberate language URLs and hreflang relationships when alternate translations genuinely exist.", "Robots directives should protect private or duplicate areas without blocking required assets. A sitemap should contain canonical, indexable URLs and be regenerated when content changes. Validate both with Search Console and a crawler."]),
      section("Structured data and links", ["JSON-LD can clarify whether a page is an article, profile, product, or organization. It must describe visible, accurate content; it is not a place for attractive unsupported claims.", "Internal links establish hierarchy and crawl paths. Keep important pages in navigation or the editorial graph, use descriptive link text, and avoid thin tag pages with no independent value."]),
      section("Performance is part of the experience", ["Image dimensions, responsive formats, lazy loading below the fold, and sensible font loading reduce layout shifts and transfer. Bundle analysis can expose a library imported for one small feature. Split routes and defer non-critical code, then verify interaction.", "Core Web Vitals are signals, not a substitute for usability. Measure real-user data where possible and test slower devices. Smaller JavaScript, stable layout, fast response, and useful HTML help both users and discoverability."]),
      section("The launch checklist", ["Before launch: confirm intent, headings, unique metadata, canonicals, indexability, accessible links, alt text, structured data, and responsive rendering. At launch: submit the sitemap, inspect URLs, check redirects, verify monitoring, and test production rather than localhost.", "After launch: monitor indexing, crawl errors, duplicate titles, structured-data warnings, Core Web Vitals, broken links, and content quality. Strong technical SEO cannot compensate for weak or duplicated content."]),
    ]
  }),
  makeArticle({
    slug: "vue-design-system-engineering", title: "Building a Reusable Vue.js Design System Without Slowing the Team Down", category: "Frontend Architecture", tags: ["Vue.js", "Design Systems", "Frontend Architecture", "Component APIs", "Developer Experience"], excerpt: "A design system should accelerate delivery without becoming a rigid internal framework. This case study covers component boundaries, design tokens, documentation, accessibility, and the decisions that make reuse practical.", seoTitle: "Building a Reusable Vue.js Design System | Ali Almanea", seoDescription: "An engineering case study on Vue.js design systems, component APIs, design tokens, accessibility, documentation, and adoption.", icon: faCode, body: [
      section("Start with repeated decisions", ["Copying a button is not the real problem. The cost appears when spacing, focus behavior, loading states, and responsive rules drift across copies. A design system starts by auditing repeated interface decisions and asking which deserve a shared contract.", "This reflects verified experience building a reusable Vue.js design system. An approximate 40% improvement in UI delivery speed was documented in that context; it is not a universal promise and depends on adoption, scope, and measurement."]),
      section("A useful hierarchy", ["Design tokens → UI primitives → shared components → product patterns → page composition. Tokens define color, spacing, type, and motion. Primitives provide accessible building blocks. Shared components combine them around a stable purpose. Product patterns solve a repeated workflow without pretending every screen is identical.", "Keeping layers distinct prevents a card from absorbing business rules or a token file from becoming arbitrary exceptions. The closer a component is to page composition, the more context it should understand."]),
      section("Component APIs should be boring", ["Expose the decisions consumers genuinely need: content slots, states, accessible labels, and a small set of variants. Excessive configuration moves complexity into every caller. Prefer composition when a component has too many boolean props.", "Responsive behavior should be safe by default. Keyboard navigation, focus visibility, disabled states, and loading announcements belong in the component contract rather than every team’s checklist."]),
      section("Documentation and testing", ["Examples are more valuable than an inventory. Document when to use a component, when not to use it, its states, and a copyable example. Visual tests catch token drift; interaction tests verify keyboard and loading behavior; type checks protect the API.", "A system also needs release and deprecation rules. Breaking changes should be visible, migration guidance practical, and consumers given a safe path."]),
      section("Adoption is a product problem", ["Teams adopt shared components when they solve a current problem better than local code. Start with frequent patterns, partner with product engineers, and measure usage and delivery time. A library nobody can find or understand is not reusable.", "Avoid over-abstraction. If two screens look similar but have different behavior and ownership, forcing one configurable component may slow both teams. Duplication can be cheaper than a premature abstraction."]),
      section("The practical test", ["A healthy system makes the common path fast, the accessible path normal, and the unusual path possible without fighting the framework. Review boundaries, keep examples close to implementation, and remove unused APIs.", "Success is not the number of components. It is whether product engineers ship consistent interfaces with less repeated reasoning while retaining ownership of the user experience."]),
    ]
  }),
];

export default articles;
