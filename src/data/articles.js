// Future entries should provide: slug, title, excerpt, publicationDate,
// readingTime, category, tags, coverImage, canonicalUrl, seoTitle,
// seoDescription, and status ("draft" or "published").
const articles = [];

export default articles;
